user nbxyz;
worker_processes auto;
pid /var/run/nginx/nginx.pid;
error_log /var/log/nginx/error.log warn;

events {
    worker_connections 1024;
}

http {
    access_log /var/log/nginx/access.log;
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    server {
        # Explicit IP+port bind — we run with docker network_mode=host
        # so the container uses NAS's network stack directly. Pinning
        # to 10.10.5.10:8080 avoids any collision with Traefik (which
        # binds 10.10.5.20:80) and keeps the external URL stable:
        # iPXE menus reference http://10.10.5.10:8080/... (clients
        # unchanged).
        listen 10.10.5.10:8080 default_server;
        server_name _;

        # ─── Pure static serving — no proxies, no caching magic ────
        #
        # Every asset referenced by the PXE menus is pre-downloaded
        # to /mnt/tank/system/pxe/http/ on the NAS (bind-mounted here
        # at /srv/http). Curl (in the download script) handles HTTPS
        # + redirects; iPXE never has to.
        #
        # Operator adds a new distro/tool: run pxe-download.sh with
        # the URL, edit the menu to point at the local path, apply.

        root /srv/http;
        autoindex on;
        autoindex_exact_size off;
        autoindex_localtime on;

        sendfile on;
        tcp_nopush on;
        gzip off;

        # One location block handles every URL path. nginx walks
        # /srv/http matching the URI; if a file exists there, serves
        # it. Otherwise 404.
        location / {
            try_files $uri $uri/ =404;
        }
    }
}
